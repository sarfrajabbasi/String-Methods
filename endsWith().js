let str = "if you don't want to die then hand over me this malaaitikka"//its a non-veg chicken tikka with cream.

console.log(str.endsWith('want to die '))  // false
console.log(str.endsWith('malaaitikka'))      // ture
console.log(str.endsWith('and over'))  // false